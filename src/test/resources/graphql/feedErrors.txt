query($namespace: String) {
  feed(namespace: $namespace) {
    feed_version
    error_counts {
      count
      message
      priority
      type
    }
    errors {
      bad_value
      error_id
      error_type
      error_type_priority
      entity_id
      entity_sequence
      entity_type
      line_number
    }
  }
}