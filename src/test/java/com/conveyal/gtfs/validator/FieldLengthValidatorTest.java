package com.conveyal.gtfs.validator;

import com.conveyal.gtfs.GTFS;
import com.conveyal.gtfs.GTFSFeed;
import com.conveyal.gtfs.TestUtils;
import com.conveyal.gtfs.error.SQLErrorStorage;
import org.junit.BeforeClass;
import org.junit.Test;

import java.io.IOException;

import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.MatcherAssert.assertThat;

public class FieldLengthValidatorTest {
    @Test
    public void validateFieldlLength() {
/*
        String[] args = {"-u", "blah"};
        GTFS.main(args);

        SQLErrorStorage errorStorage = new SQLErrorStorage();
        FieldLengthValidator validator = new FieldLengthValidator(null, null);
        final String longFieldValue = "abcdefghijklmnopqrstwxyz1234567890";
        final String shortFieldValue = "abcdef";
        final int length = 20;

        assertThat(validator.validateFieldLength(longFieldValue, length), is(true));
        assertThat(validator.validateFieldLength(shortFieldValue, length), is(false));
 */
    }
}
